/* tslint:disable:no-unused-variable */

import { ColorToClassPipe } from './color-to-class.pipe';

describe('Pipe: ColorToClasse', () => {
  it('create an instance', () => {
    let pipe = new ColorToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
